{
	"name": "@repo/database",
	"version": "0.0.0",
	"main": "./index.ts",
	"types": "./index.ts",
	"scripts": {
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"push": "dotenv -c -e ../../.env -e ../../.env.local -- drizzle-kit push",
		"generate": "dotenv -c -e ../../.env -e ../../.env.local -- drizzle-kit generate",
		"migrate": "dotenv -c -e ../../.env -e ../../.env.local -- drizzle-kit migrate",
		"studio": "dotenv -c -e ../../.env -e ../../.env.local -- drizzle-kit studio",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@paralleldrive/cuid2": "^3.3.0",
		"@repo/logs": "workspace:*",
		"dotenv": "catalog:",
		"drizzle-orm": "catalog:",
		"drizzle-zod": "catalog:",
		"pg": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@repo/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"dotenv-cli": "catalog:",
		"drizzle-kit": "catalog:"
	}
}
